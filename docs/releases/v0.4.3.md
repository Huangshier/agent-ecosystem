# v0.4.3 Release Notes

Status: release prep draft

`v0.4.3` is planned as a post-convergence stabilization release. It prepares
the public repository after the `v0.4.1` template-authority consolidation and
the `v0.4.2` language-scoped template convergence, before any `v0.5.0`
expansion work.

This document is a draft for maintainer review. It is not a published release,
tag, or GitHub Release.

## Highlights

- `v0.4.1` and `v0.4.2` release records are normalized across README
  entrypoints, changelog, release readiness, release notes, and release
  validation.
- Legacy template-path references are audited. Active setup guidance points to
  `templates/languages/<language>/project-root|project-agent`, while historical
  references are marked as legacy, removed, superseded, or historical state.
- Existing `.agents` projects have a conservative upgrade guide that preserves
  project-specific memory and runtime-generated context while using an
  analyze -> plan -> backup -> apply -> validate flow.
- Release validation covers the new release records, legacy path audit, upgrade
  path guidance, and this release-prep note.

## Linked Issues and PRs

- Issue #53: normalize `v0.4.1` / `v0.4.2` release records.
- Issue #54: audit legacy template-path references after language-scoped
  convergence.
- Issue #55: document the post-`v0.4.2` upgrade path for existing `.agents`
  projects.
- Issue #56: define domain-pack governance lifecycle without enabling expansion
  (deferred until after stabilization review).
- Issue #57: prepare `v0.4.3` stabilization release.

## Validation Surface

Release-prep local validation result:

```text
PASS=46 FAIL=0 WARN=0 DEFERRED=0
```

The release gate covers all prior checks plus:

- `v0.4.1`, `v0.4.2`, and `v0.4.3` release note coverage
- legacy template-path reference audit coverage
- existing project upgrade path coverage
- public/private boundary scan

Hosted release validation must pass on the draft PR before maintainer merge.

## Human Release Decisions

Before publishing, the maintainer should decide:

- whether the stacked stabilization PRs should merge in order;
- whether the `v0.4.3` changelog entry and release notes need wording edits;
- whether hosted release validation evidence is sufficient;
- whether to tag `v0.4.3`;
- whether to publish the GitHub Release.

Do not tag or publish from this draft without maintainer approval.

## Public Boundary

This release-prep draft does not publish private overlay content, local
migration mappings, machine-specific paths, private review material, or
local-only automation material.

The project remains a Workflow Kernel. This release does not add a real public
domain pack, promote `embedded-core` to a scriptable skill, or change `full` /
`dev` profile behavior.
