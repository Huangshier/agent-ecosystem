# 测试约定

## Summary

记录项目的测试框架、命令、环境要求、fixture 模式、覆盖率期望和手工验证
步骤。本文件是项目特定的；请填写当前项目实际使用的约定。

## Keywords

test framework, test command, coverage, fixtures, CI test, manual verification

## 框架

记录项目使用的测试框架。示例：pytest、Jest、go test、cargo test、JUnit、
Google Test、Catch2、Unity (C)。

| 字段 | 值 |
|------|------|
| 主要框架 | *（填写）* |
| 语言 / 运行时 | *（填写）* |
| 配置文件 | *（填写）* |

## 测试命令

| 操作 | 命令 |
|------|------|
| 运行全部测试 | *（填写）* |
| 运行单个测试文件 | *（填写）* |
| 运行单个测试用例 | *（填写）* |
| 运行覆盖率检查 | *（填写）* |

## 环境

记录运行测试套件前所需的环境变量、服务、数据库、硬件 fixture 或外部依赖。

- *（填写，或写"无——测试无需特殊设置即可运行"）*

## Fixture 和测试数据

描述项目的 fixture 模式：

- 测试 fixture 存放位置（目录、命名规则）。
- 测试数据如何生成或维护。
- 测试使用 mock、stub、fake 还是真实服务。

*（填写）*

## 覆盖率

| 指标 | 目标 | 度量方式 |
|------|------|----------|
| 行覆盖率 | *（填写或"未跟踪"）* | *（工具名）* |
| 分支覆盖率 | *（填写或"未跟踪"）* | *（工具名）* |

## 手工验证

列出需要手工执行的验证步骤（如烧录固件、连接硬件、检查浏览器 UI）：

- *（填写，或写"无——所有验证均为自动化"）*

## CI 集成

记录测试在 CI 中如何运行：

| CI 系统 | Workflow / Job 名称 | 触发条件 |
|---------|---------------------|----------|
| *（填写）* | *（填写）* | *（填写）* |

## 说明

- 本模板不强制使用任何特定测试框架。
- 只填写与当前项目相关的部分。
- 项目测试方式变化时更新本文件。
- 如果项目还没有自动化测试，记录手工验证方式和最小可行的测试性改进，不要阻断
  无关工作。
