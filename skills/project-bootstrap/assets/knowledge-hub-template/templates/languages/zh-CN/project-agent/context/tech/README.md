# 技术上下文

项目记忆语言：简体中文。

在这里记录稳定的技术知识。

## 条目索引

| 文件 | 摘要 | 关键词 |
| --- | --- | --- |
| `testing-conventions.md` | 项目特定的测试框架、命令、fixtures、覆盖率、CI 和手工验证约定。 | testing, test command, coverage, fixtures, CI |

保持该索引由人工维护，并只记录 public-safe discovery metadata。只有在 `Maturity` 或
`Reviewed` 由人工维护时，才允许加入这些可选列；不要包含 runtime telemetry、usage counts、
`last_accessed` 或 automatic decay state。

建议章节：
1. Background（背景）
2. Constraints（约束）
3. Decision（决策）
4. Verification（验证）
