Current State
- This repository is the public source repository for Agent Ecosystem.
- Current published public release is `v0.3.1`.
- Active public maintenance issue:
  - #32, add file-based `en` and `zh-CN` engineering-memory templates.
- Active public branch: `issue-32-file-based-memory-templates`.
- Active public spec:
  `docs/specs/file-based-memory-templates/spec.md`.
- Public engineering memory is English-first.
- Private migration state, local source mappings, sensitive audit notes, and
  personal overlay details live outside this repository.
- `knowledge-hub/` is tracked as a normal directory inside this repository, not
  as a nested Git repository or submodule.
- GitHub App `agent-ecosystem-bot` is the configured automation identity for
  this repository. Do not store App auth material or local-only paths in this
  public repository.

Completed
- `v0.1.0`, `v0.2.0`, `v0.3.0`, and `v0.3.1` have been published as GitHub
  Releases.
- Public `scripts/validate-release.ps1` is the reusable release gate.
- Release validation workflow has passed on Windows PowerShell 5.1, Windows
  PowerShell 7, Ubuntu, and macOS.
- PR #20 added minimal agent governance docs/templates and closed issue #19.
- PR #24 normalized `v0.3.1` release readiness evidence and closed issue #21.
- PR #26 documented the installed GitHub App identity and closed issue #25.
- PR #28 added the end-to-end minimal project adoption walkthrough and closed
  issue #22.
- PR #34 documented validation-tier policy and closed issue #27.
- PR #35 protected project memory upgrades and closed issues #29 and #31.
- PR #37 added the PR-ready / phase-close engineering-memory sync gate and
  closed issue #36.
- PR #39 added validation scratch pruning and closed issue #38; local `main`
  was fast-forwarded to the merge commit and the old #38 topic branch was
  cleaned locally and remotely.
- PR #40 clarified `project-bootstrap` operating modes and closed issue #33;
  local `main` was fast-forwarded to merge commit
  `19656e5f92264a960c8e6ac6039debd97166c10f`.
- Repository ruleset `protect-main` protects the default branch with required
  pull requests, release validation checks, conversation resolution, deletion
  blocking, force-push blocking, and no bypass actors.

Next Actions
- Commit, push, and open a draft PR that references `Fixes #32`.
- #32 implementation and local validation are complete:
  - file-based project memory templates exist for `en` and `zh-CN`;
  - `set_project_language.ps1` reads templates from files instead of embedded
    scaffold strings;
  - missing `zh-CN` template files fall back to English with fallback metadata;
  - bootstrap lock metadata records language template fallbacks;
  - docs and release validation were updated;
  - local release validation passed with `PASS=37 FAIL=0 WARN=0 DEFERRED=0`.
- Keep #30 conservative language migration apply for a separate later PR.
- Do not merge directly to `main`.

Blocking Issues
- No active blockers.

Last Updated
- 2026-05-13
