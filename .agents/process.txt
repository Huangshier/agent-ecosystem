Current State
- This repository is the public source repository for Agent Ecosystem.
- Current published public release is `v0.4.2`.
- Active work: restore #59 legacy template path audit and #60 existing project
  upgrade path content onto `main` after stacked PRs were merged into non-main
  base branches.
- Active public branch: `codex/restore-stabilization-audit-upgrade-docs`.
- Public engineering memory is English-first.
- Private migration state, local source mappings, sensitive audit notes, and
  personal overlay details live outside this repository.
- `knowledge-hub/` is tracked as a normal directory inside this repository, not
  as a nested Git repository or submodule.
- GitHub App `agent-ecosystem-bot` is the configured automation identity for
  this repository. Do not store App auth material or local-only paths in this
  public repository.
- Repository ruleset `protect-main` protects the default branch.

Completed
- `v0.1.0`, `v0.2.0`, `v0.3.0`, `v0.3.1`, `v0.4.0`, and `v0.4.1` have been
  published as GitHub Releases.
- `v0.4.2` has been published as a GitHub Release.
- Public `scripts/validate-release.ps1` is the reusable release gate.
- Release validation workflow has passed on Windows PowerShell 5.1, Windows
  PowerShell 7, Ubuntu, and macOS for prior release heads.
- PR #50 released v0.4.1 and moved project-memory language templates into the
  intermediate v0.4.1 authority layout.
- Issue #51 moved the template model to
  `knowledge-hub/templates/languages/en|zh-CN/project-root|project-agent` and
  the matching bundled snapshot under
  `skills/project-bootstrap/assets/knowledge-hub-template/templates/languages/`.
- PR #52 was merged and released as `v0.4.2`:
  https://github.com/Huangshier/agent-ecosystem/pull/52
- Local requested validation for #51 passed:
  - `git diff --check`
  - `powershell -NoProfile -ExecutionPolicy Bypass -File scripts/validate-release.ps1`
    with `PASS=40 FAIL=0 WARN=0 DEFERRED=0`
- Stabilization issues are open:
  - #53 normalize `v0.4.1` / `v0.4.2` release records.
  - #54 audit legacy template-path references.
  - #55 document the post-`v0.4.2` upgrade path for existing `.agents` projects.
  - #56 define domain-pack governance lifecycle without enabling expansion
    (deferred).
  - #57 prepare the `v0.4.3` stabilization release.

Next Actions
- Validate the restore branch from current `origin/main`.
- Open a draft PR to `main` titled
  `[codex] Restore stabilization audit and upgrade docs on main`.
- Do not push directly to `main`.

Latest Local Validation
- Pending for the restore branch.

Blocking Issues
- No active blockers.

Last Updated
- 2026-05-14
